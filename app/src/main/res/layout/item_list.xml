<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="8dp">

    <ImageView
        android:id="@+id/Motor_image"
        android:layout_width="wrap_content"
        android:src="@mipmap/ic_launcher"
        android:layout_margin="8dp"
        android:layout_height="wrap_content"
        android:contentDescription="@string/Motor_image_desc" />
    <TextView
        android:id="@+id/Motor_name"
        android:layout_toRightOf="@id/Motor_image"
        android:layout_marginBottom="8dp"
        style="@style/Motor_name_style"
        android:layout_toEndOf="@id/Motor_image" />
    <TextView
        android:id="@+id/Motor_description"
        android:layout_toRightOf="@id/Motor_image"
        android:layout_below="@id/Motor_name"
        style="@style/Motor_description_style"
        android:layout_toEndOf="@id/Motor_image" />

</RelativeLayout>